# Default model per workflow step. Orchestrator passes to Task child agents.
# All step executions use composer-2.5 unless a step entry overrides (none today).

defaults:
  model: composer-2.5
  thinking: medium

orchestrator:
  model: composer-2.5
  thinking: medium

# Step child agents inherit defaults (composer-2.5). Add per-step entries only to override.

steps: {}
